#pragma once
#include <SFML/Graphics.hpp>

enum AnimationPlayer
{
    // JUMPING_X = 0,
    // MOVING_X = 0,
    // JUMPING_Y = 42,
    // MOVING_Y = 4,
    // STOPP_JUMPING = 36,
    // STOP_MOVING = 4,
    // SCALE = 2,
    // TILE_SIZE = 32,
    // TILE_SIZE_Y = 35

    JUMPING_X = 0,
    MOVING_X = 0,
    JUMPING_Y = 41,
    MOVING_Y = 4,
    STOPP_JUMPING = 41,
    STOP_MOVING = 4,
    TILE_SIZE = 32
};